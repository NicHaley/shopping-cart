// This view will render some JavaScript as a response to an item being added to the cart

<% if @order.errors.any? || @order_item.errors.any? %>
	alert("not valid.")
<% else %>
	$(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
<% end %>